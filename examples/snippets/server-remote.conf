# Personal [server_remote] fragment.
# Replace the placeholder with the private Nexitally Quantumult X URL
# that Quantumult X already has on the device. Never commit the real URL.

[server_remote]
<YOUR_PRIVATE_NEXITALLY_QUANTUMULT_X_URL>, tag=Nexitally, opt-parser=true, update-interval=21600, enabled=true
