# Paste under [server_remote] in a local Quantumult X profile.
# Replace the URL with the private Nexitally Quantumult X full-configuration URL.
# Never commit the real URL.

[server_remote]
<YOUR_PRIVATE_NEXITALLY_QUANTUMULT_X_URL>, tag=Nexitally, opt-parser=true, update-interval=21600, enabled=true
