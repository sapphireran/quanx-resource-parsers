# Full configuration that never declares [server_local].
# The parser must fail instead of inventing nodes from other sections.

[general]
server_check_url = http://www.apple.com/generate_204

[server_remote]
https://example.invalid/other-subscription, tag=Other, enabled=false

[policy]
static = Empty, direct

[filter_local]
final, direct
