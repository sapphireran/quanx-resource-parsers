# Synthetic download that is not a managed Quantumult X node list.
# The parser must return the [server_local] missing error.

[general]
server_check_url = http://www.apple.com/generate_204

[dns]
no-ipv6
server = 223.5.5.5

[policy]
static = Proxy, direct

[server_remote]
https://example.com/other.snippet, tag=Other, enabled=false

[filter_local]
final, Proxy
