[general]
server_check_url = http://www.apple.com/generate_204

[server_local]
; only comments live here
# leftover header from a managed download
// still not a server line

[policy]
static = Empty, img-url=https://example.invalid/empty.png
