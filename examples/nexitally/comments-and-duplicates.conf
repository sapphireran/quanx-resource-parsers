# Only one unique server should survive. Three comment styles, surrounding
# whitespace, and an exact duplicate are all present.

[server_local]

  # hash comment with leading spaces

; semicolon comment

// slash comment

anytls=only-01.example.test:443, password=example-password, over-tls=true, tls-host=www.apple.com, udp-relay=true, tag=ONLY-01

  anytls=only-01.example.test:443, password=example-password, over-tls=true, tls-host=www.apple.com, udp-relay=true, tag=ONLY-01  

[filter_local]
final, direct
