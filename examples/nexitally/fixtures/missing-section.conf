# No [server_local] at all: the download was HTML, Clash YAML, or a
# server-only list without a section header.

[general]
server_check_url = http://www.apple.com/generate_204

[policy]
static = unused, Sample-A
