# Paste into the [general] section of a private Quantumult X profile.
# Pinning a commit SHA is safer than tracking @main if you want the
# parser to stop changing under you.

[general]
; jsDelivr (commit-pin this in a real profile)
resource_parser_url = https://cdn.jsdelivr.net/gh/sapphireran/quanx-resource-parsers@main/nexitally-node-parser.js

; GitHub raw is the same file. Use one line, not both.
; resource_parser_url = https://raw.githubusercontent.com/sapphireran/quanx-resource-parsers/main/nexitally-node-parser.js
