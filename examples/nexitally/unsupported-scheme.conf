# Schemes Quantumult X server resources do not take from this parser.
# One supported line must remain so the result is success, not the
# empty-section error.

[server_local]
wireguard=wg.example.test:51820, private-key=example, tag=WG-01
hysteria=hy.example.test:443, password=example-password, tag=HY-01
tuic=tuic.example.test:443, password=example-password, tag=TUIC-01
anytls=keep-01.example.test:443, password=example-password, over-tls=true, tls-host=www.apple.com, udp-relay=true, tag=KEEP-01

[policy]
static = Proxy, direct
