# Fictional download that is not a Quantumult X full configuration.
# The parser must fail because [server_local] is absent.

[general]
server_check_url = http://www.example.com/generate_204

[policy]
static = Final, direct

[filter_local]
final, direct
