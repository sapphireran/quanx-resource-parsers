# Local [general] fragment.
# Quantumult X loads exactly one resource_parser_url. Point it at this
# repository's Nexitally parser (or a raw.githubusercontent.com equivalent).

[general]
resource_parser_url = https://cdn.jsdelivr.net/gh/sapphireran/quanx-resource-parsers@main/nexitally-node-parser.js
server_check_url = http://www.apple.com/generate_204
server_check_timeout = 5000
