# Full-looking configuration that never declares [server_local].
# The parser must error instead of inventing servers from [policy] names.

[general]
server_check_url = http://www.apple.com/generate_204

[dns]
server = 223.5.5.5

[policy]
static = PROXY, direct

[server_remote]
https://example.invalid/other.snippet, tag=Other, enabled=false

[filter_local]
host, www.apple.com, direct
