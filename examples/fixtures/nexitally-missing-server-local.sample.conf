# Error fixture: a full configuration that never declares [server_local].

[general]
server_check_url = http://www.apple.com/generate_204

[dns]
server = 223.5.5.5

[policy]
static = Empty, PROXY, DIRECT

[server_remote]
https://private.example.test/should-not-be-parsed.conf, tag=Wrong-Section, enabled=false

[filter_local]
final, proxy
