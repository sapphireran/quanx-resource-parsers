# Changelog

## Unreleased

- Personal operator handbook (`handbook/`) covering scope, device wiring, keep/drop reason codes, and a Chinese index.
- Replay studio (`studio/`) that evaluates the committed parser in a Node sandbox, prints per-line reason codes, and writes a generated tracebook.
- Sanitized fixtures for Nexitally-style full profiles, exclusion substring traps, section fences, and official Quantumult X protocol samples.
- README `resource_parser_url` now points at `sapphireran/quanx-resource-parsers`.
