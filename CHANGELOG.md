# Changelog

Personal notes for this repository. Dates are commit dates on `main`.

## Unreleased

- Add a documentation set under `docs/` covering the official resource-parser API, the Nexitally extract pipeline, the personal Quantumult X workflow, troubleshooting, privacy, compatibility, and how to add another full-config parser.
- Add sanitized fixtures under `examples/nexitally/` plus `examples/run-parser.js`, a Node stand-in for `$resource` / `$done`. The catalog covers the happy path, header/newline edge cases, exclusions, HTML, and Clash YAML.
- Expand the root README with a repository map, canonical parser URLs for `sapphireran/quanx-resource-parsers`, and a pointer at the example catalog.
- Add `examples/local-profile-snippet.conf` as a copy-paste `[general]` / `[server_remote]` fragment with a placeholder URL.

## 2026-07-14

- Add `nexitally-node-parser.js`.
- Expand the README with usage, privacy warnings, and AnyTLS compatibility notes.
