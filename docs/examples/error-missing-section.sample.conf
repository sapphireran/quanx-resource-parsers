# Error fixture: a Quantumult X profile with no [server_local] section.
# The parser must fail instead of turning [policy] or [filter_local]
# into fake server lines.

[general]
server_check_url = http://www.apple.com/generate_204

[policy]
static = Proxy, direct

[filter_local]
geoip, cn, direct
final, Proxy
